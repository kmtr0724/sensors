name: Raspberrypi-github-actions

on:
 push:
  branches: [ "main" ]

jobs:
  raspberrypi-test:
    runs-on: [self-hosted, linux, arm]
    steps:
      - name: Run a one-line script
        run: |
          echo "Hello, world!" > /home/ryu/helloworld.txt
          pwd
